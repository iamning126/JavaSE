super注意点
    1、super调用父类的构造方法，必须在构造方法的第一个
    2、super必须只能出现在子类的方法或者构造方法（构造器）
    3、super和this 不能同时调用构造方法！
VS this关键字:
    this:本身调用的这个对象
    super：代表父类对应的引用
前提条件
    this：没有继承也可以使用
    super：只能在继承条件才可以使用
构造方法
    this（）： 本类的构造
    super（）：父类的构造
    //这里有个快捷键ctrl+f
    用来寻找源码里的关键字
重写：
    条件：1、需要有继承关系且一定是子类重写父类
         2、方法名必须相同
         3、参数列表必需相同
         4、修饰符：范围可以扩大：public>protected>default>private
         5、抛出异常：范围，可以缩小，但不能扩大：ClassnotFoundException--->(大)Exception
    重写，子类和父类必须要一致，方法体不同!
    为什么需要重写：
    1、父类的功能，子类不一定需要，或者不一定满足！
    alt + insert ：override 或者 ctrl + o
